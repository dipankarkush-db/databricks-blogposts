# Pinned to the Delta Lake 4.2 release versions.
# - Apache Spark 4.1
# - Delta Spark 4.2.0
# - unitycatalog-spark 0.4.1 (pulled by Spark via --packages, not pip)
# - DuckDB 1.5.1 with the unity_catalog + delta core extensions
#   (see https://duckdb.org/docs/current/core_extensions/unity_catalog).
#   Pinned to 1.5.1 (not the latest 1.5.2) because the `delta` extension
#   bundled with DuckDB 1.5.2 (build 21dfabe) regresses on catalog-managed
#   Delta tables — every load fails with `Catalog-managed table requires
#   max_catalog_version to be set`. The 1.5.1 build (df4a08d) reads + writes
#   UC managed Delta tables cleanly with the same `unity_catalog@0202409`
#   extension. Move back to latest once the upstream `delta` extension is
#   fixed.
pyspark==4.1.1
delta-spark==4.2.0
duckdb==1.5.1
pandas>=2.2.0
databricks-sdk>=0.30.0
